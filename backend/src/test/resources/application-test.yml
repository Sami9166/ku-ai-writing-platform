spring:
  datasource:
    url: jdbc:h2:mem:ku_ai_trace;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
ku:
  assignment-id: 1
  groq:
    api-key:
    dotenv-enabled: false
  google:
    api-key:
  professor:
    provider: groq
